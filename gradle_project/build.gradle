apply plugin: 'java'
apply plugin: 'eclipse'

version = "1.0"
jar {
	manifest {
		attributes "Implementation-Title":"Gradle javaPlugin",
					"Implementation-Version": version
	}
}

test {
	systemProperties "property" : "我是谁"
}


repositories {
	mavenCentral()
}

dependencies {
	compile group: "junit", name: "junit", version: "4.12"
}

uploadArchives {
    repositories {
       flatDir {
           dirs 'repos'
       }
    }
}

